<div class="settings-nav col-sm-3">
    <ul class="nav nav-pills nav-stacked">
     {% for cli in clients %}
        <li class="profile glyph dropdown"><a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" id="drop4">{{ cli['name'] }}
        <span>{{ cli['total'] }}</span>
        <span class="caret"></span>
        </a>
            <ul aria-labelledby="drop4" role="menu" class="dropdown-menu" id="menu1">
                <li role="presentation"><a href="{{ path('lc_not_inbox',{'id' : cli['id']}) }}" tabindex="-1" role="menuitem">{{ 'inbox'|trans }}</a></li>
                <li role="presentation"><a href="{{ path('lc_not_config',{'id' : cli['id']}) }}" tabindex="-1" role="menuitem">{{ 'configure'|trans }}</a></li>
            </ul>
        </li>
    {% endfor %}
    </ul>
</div>
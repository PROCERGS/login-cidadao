{% extends "PROCERGSLoginCidadaoCoreBundle::base.loggedIn.html.twig" %}

{% block content %}
    <div class="content notifications-broadcast-new">
        <h2>{{ 'Notification Broadcast' | trans }}</h2>

        <div class="row">
            <div class="col-sm-10 col-sm-offset-1">
                {{ form_start(form) }}
                <div class="panel panel-default">
                    <div class="panel-body">
                        {{ form_row(form.category) }}
                        {{ form_row(form.receivers) }}
                    </div>
                    <div class="panel-footer text-right">
                        <input class='btn btn-success' type="submit" value="{{ 'Next'|trans }}" />
                    </div>
                </div>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock content %}